<!DOCTYPE html>
<html>
  <head>
    <style>
    table {
        font-family: arial, sans-serif;
        border-collapse: collapse;
        width: 100%;
    }

    td, th {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 8px;
    }

    tr:nth-child(even) {
        background-color: #dddddd;
    }
    </style>
  </head>
  <body>

    <table>
      <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Email</th>
        <th></th>
      </tr>
      <% @User.each do |user| %>
      <tr>
        <td><%= user.first_name%></td>
        <td><%= user.last_name%></td>
        <td><%= user.email_address%></td>
        <td> <a href="/users/show/<%= user.id%>">Show</a> | <a href="/users/edit/<%= user.id%>">Edit</a> | <a href="/users/destroy/<%= user.id%>">Destroy</a></td>
      </tr>
      <% end %>
    </table>

    <p><a href="/users/new"> Add New User</a></p>
  </body>
</html>
